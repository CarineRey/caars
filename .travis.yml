language: c
sudo: required
install: wget https://raw.githubusercontent.com/CarineRey/caars/master/etc/travis.sh
script: bash -ex .travis-opam.sh
env:
  - OCAML_VERSION=4.03.0
  - OCAML_VERSION=4.04.1
os:
  - linux
